// List of Enums
// Cannot be created as a class

enum TextSizes { small, medium, large }

enum OrderStatus { pending, processing, shipped, delivered }

enum PaymentMethod { visa, mastercard, creditCard, debitCard, netBanking, cashOnDelivery }
